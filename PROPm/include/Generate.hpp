#pragma once

#include <vector>
#include <string>

using namespace std;

vector<vector<int>> generate_random_matrix(int n, int m);

void generate_sample(string filename_out, int count, int agent_number, int, string, int type);